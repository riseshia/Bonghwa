{
  fws: firewoods.map do |fw|
    {
      id: fw.id,
      is_dm: fw.is_dm,
      prev_mt_id: fw.prev_mt_id,
      root_mt_id: fw.root_mt_id,
      user_id: fw.user_id,
      name: fw.user_name,
      contents: fw.contents,
      image_url: fw.image_url,
      image_adult_flg: fw.image_adult_flg,
      created_at: fw.formatted_created_at
    }
  end,
  users: users,
  infos: infos.map do |info|
    { id: info.id, information: info.infomation }
  end
}
